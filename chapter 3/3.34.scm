

x = a * a
b = x


because input a is the same connector it will never be able to set the value of a given product given one input

(define (squarer a b)
  (multiplier a a b))

given b you can't get a as neither m1 or m2 will be set 